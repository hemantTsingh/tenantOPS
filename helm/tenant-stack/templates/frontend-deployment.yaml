apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenantops-frontend
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.frontend.replicas }}
  selector:
    matchLabels:
      app: tenantops-frontend
  template:
    metadata:
      labels:
        app: tenantops-frontend
    spec:
      containers:
        - name: frontend
          image: {{ .Values.frontend.image }}:{{ .Values.frontend.tag }}
          ports:
            - containerPort: {{ .Values.frontend.port }}
